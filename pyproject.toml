[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "samg"
version = "1.0.0"
description = "SAM-G"
readme = "README.md"
requires-python = ">=3.9"

[tool.hatch.build.targets.wheel]
packages = ["src/samg/"]
